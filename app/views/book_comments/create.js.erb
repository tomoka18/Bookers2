$("#book_<%= @book.id %> .comments-count").html("コメント数: <%= @book.book_comments.count %>");
$('.comments_index').html("<%= j(render 'books/comments', book: @book, user: @user) %>", );
$(".text_field").val("");